<%= form_for(@notification, :url => notification_path(@notification)) do |f| %>
  <% if @notification.errors.any? %>
   <div id="error_explanation">
      <h2><%= pluralize(@notification.errors.count, "error") %> prohibited this hoge from being saved:</h2>

      <ul>
      <% @notification.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 
  <table>
    <% @notification.attributes.keys.each do |key| %>
    <tr>
     <td> <%= key %></td>
    </tr>
    <% end %>
  </table>
  <div class="field">
    <%= f.label :data %><br />
    <%= f.text_field :data %>
  </div>
  <div class="actions">
  <%= f.submit %>
  </div>
<% end %>
